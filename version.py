"""Single source of truth for the VoxSlide AI application version.

Bump __version__ when you publish a release, then update the matching
#define MyAppVersion in installer.iss (see RELEASE.md).
"""

__version__ = "1.0.1"
